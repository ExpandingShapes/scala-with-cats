package monoidsandsemigroups.exercises

/**
 * 2.4 What monoids and semigroups are there for sets?
 */
object AllSetForMonoidsApp extends App {
//  val setSemigroupintersect = Semigroup(
//    new Semigroup[Set[_]] {
//      override def combine(x: Set[_], y: Set[_]): Set[_] = x intersect y
//    }
//  )
//
//  val setMonoidDiff = Monoid(
//    new Monoid[Set[_]] {
//      override def combine(x: Set[_], y: Set[_]): Set[_] = x diff y
//      override def empty: Set[_] = Set.empty
//    }
//  )
}
